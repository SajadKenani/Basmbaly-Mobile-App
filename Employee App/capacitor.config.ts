import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.qafemployee.app',
  appName: 'بصملي: تطبيق الموظف',
  webDir: 'build'
};

export default config;
